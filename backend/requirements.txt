fastapi>=0.110
uvicorn[standard]>=0.29
sqlalchemy>=2.0
pydantic>=2.6
psycopg[binary]>=3.1
python-dotenv>=1.0
python-multipart>=0.0.9
httpx>=0.27
# Re-encodes admin-uploaded header/banner images in uploads.py — raw
# uploads (e.g. HEIC/AVIF straight from a phone, multi-MB PNGs) were
# silently failing to render in iMessage/SMS link previews.
Pillow>=10.0
# Gemini (via Google AI Studio's free tier) does the transcript
# summarization in ingest_youtube.py.
google-genai>=1.0
# Must be >=1.0 — 0.6.x's transcript endpoint request no longer works
# against YouTube's current backend (empty response, confirmed by hand);
# the 1.x rewrite (instance-based YouTubeTranscriptApi().fetch()) is what
# ingest_youtube.py is written against.
youtube-transcript-api>=1.0
