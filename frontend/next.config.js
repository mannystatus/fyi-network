/** @type {import('next').NextConfig} */
const nextConfig = {
  // Nothing exotic needed — each brand domain gets attached to this SAME
  // deployment in your host's dashboard (see README "Deploying" section).
  // The middleware.ts + backend brand-resolution handle the rest.
};

module.exports = nextConfig;
